{
  "name": "cdp-element",
  "version": "0.1.0",
  "description": "Standalone CLI for managing CDP elements in C++ component files",
  "main": "dist/main.js",
  "bin": {
    "cdp-element": "dist/main.js"
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
